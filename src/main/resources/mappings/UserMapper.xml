<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.ssf.dao.IUserDao">
    
    <sql id="columns">
       a.id,
       a.username,
       a.password,
       a.create_time AS 'createTime'
    </sql>
    
    <select id="findAll" resultType="User"><!-- 如果你定义了别名你就可以简化你的写法 -->
         SELECT 
         	<include refid="columns"/> 
         FROM sys_user a
    </select>
    
    <select id="findById" resultType="com.ssf.model.User">
		SELECT
			<include refid="columns"/>
		FROM sys_user a
		WHERE a.id = #{id}
	</select>
  
    <select id="findByUserName" resultType="User" parameterType="java.lang.String">
    	SELECT
			<include refid="columns"/>
		FROM sys_user a
		WHERE a.username = #{username}
    </select>
</mapper>